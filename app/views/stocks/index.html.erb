<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Stocks</title>
</head>
<body>
  <table>
    <tr>
      <th>Symbol</th>
      <th>Quantity</th>
      <th>Price Bought</th>
    </tr>
    <% @stocks.each do |stock| %>
        <tr>
          <td><%= stock.symbol %></td>
          <td><%= stock.quantity %></td>
          <td><%= stock.price %></td>
          <td><%= link_to 'Show', stock_path(stock) %></td>
          <td><%= link_to 'Edit', edit_stock_path(stock) %></td>
          <td><%= link_to 'Destroy', stock_path(stock),
                          method: :delete,
                          data: { confirm: 'Are you sure?' } %></td>
        </tr>
    <% end %>
    <%= link_to 'New stock purchase', new_stock_path %>
  </table>
</body>
</html>